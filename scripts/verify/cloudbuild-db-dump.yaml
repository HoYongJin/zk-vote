steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -f
      - scripts/verify/cloud-run-db-dump.Dockerfile
      - -t
      - ${_IMAGE}
      - .
options:
  logging: CLOUD_LOGGING_ONLY
images:
  - ${_IMAGE}
